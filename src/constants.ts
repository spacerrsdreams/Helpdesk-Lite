//! Since we want PATCH or Delete requests to be doable on newly created posts which are cached
//! We need to this as guard in the hooks.
//! Which will act as kind of mocked DB.
export const JSON_PLACEHOLDER_MAX_TICKETS = 100;
export const JSON_PLACEHOLDER_MAX_NOTES = 5;
